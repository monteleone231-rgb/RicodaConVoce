<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- SCHEDULE_EXACT_ALARM (Android 12/13+): Required for exact scheduling. Checked at runtime. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- WAKE_LOCK: Required to wake the CPU and keep it active while triggers boot -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- USE_FULL_SCREEN_INTENT: Required to launch high-priority notifications on top of lockscreen -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" tools:ignore="UseFullScreenIntent" />

    <!-- RECEIVE_BOOT_COMPLETED: Required to reschedule alarms after mobile restarts -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Android 13/14+ Foreground Service and Post Notifications permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Main Onboarding & Reminder Screen -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.App.Starting"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Full-Screen Alerts Page popped up on screen activation -->
        <activity
            android:name=".FullScreenAlertActivity"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:exported="false" />

        <!-- Foreground alert audio loop service -->
        <service
            android:name=".ReminderAlertService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Voice reminders and alarms requiring immediate full-screen vocal and audio alerting when the device is locked" />
        </service>

        <!-- Alarm receiver from AlarmManager system broadcast and notification actions -->
        <receiver
            android:name=".AlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.ricordaconvove.ACTION_MARK_TAKEN" />
                <action android:name="com.ricordaconvove.ACTION_SNOOZE" />
            </intent-filter>
        </receiver>

        <!-- Boot receiver to reschedule on power up -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
